# Dipiri Landing

Standalone shutdown landing page for Dipiri. As the project is shutdown for now

## Files

- `index.html` - landing page markup
- `styles.css` - page styling
- `assets/` - copied brand assets used by the landing page

## Deploy

This can be deployed directly as a static site on Vercel.
